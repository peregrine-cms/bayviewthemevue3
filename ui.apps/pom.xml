<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.bayviewthemevue3</groupId>
        <artifactId>bayviewthemevue3-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>bayviewthemevue3-ui.apps</artifactId>
    <packaging>content-package</packaging>

    <name>Bayview Theme - UI Apps</name>
    <description>JCR content package for Bayview Theme (components, templates, felibs)</description>

    <build>
        <resources>
            <!-- JCR content -->
            <resource>
                <directory>${basedir}/src/main/content/jcr_root</directory>
                <excludes>
                    <exclude>**/.vlt</exclude>
                    <exclude>**/.vltignore</exclude>
                    <exclude>**/.gitignore</exclude>
                    <exclude>**/*.iml</exclude>
                    <exclude>**/.classpath</exclude>
                    <exclude>**/.project</exclude>
                    <exclude>**/.settings/**</exclude>
                    <exclude>**/.DS_Store</exclude>
                    <exclude>**/target/**</exclude>
                </excludes>
            </resource>
            <!-- Vault META-INF -->
            <resource>
                <directory>${basedir}/src/main/content/META-INF</directory>
                <targetPath>../vault-work/META-INF</targetPath>
            </resource>
            <!-- Compiled Vue 3 components (from npm build) -->
            <resource>
                <directory>${basedir}/target/classes/etc/felibs</directory>
                <targetPath>etc/felibs</targetPath>
            </resource>
        </resources>

        <plugins>
            <!-- Frontend build: install Node/npm and build Vue 3 components -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <configuration>
                    <workingDirectory>${basedir}</workingDirectory>
                    <installDirectory>${basedir}/../node</installDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <configuration>
                            <!--
                                v24 is the current LTS line (Krypton). node is
                                build-time only — the runtime image ships Java 25
                                and contains no node at all — so this affects the
                                toolchain and nothing that gets deployed.

                                Pinned ahead of the peregrine-cms parent, which
                                still sets node.version to v12.18.3.

                                npmVersion is pinned to the one node 24.19.0
                                bundles. Leaving it unset looks tidier but this
                                plugin version then installs the node binary
                                WITHOUT an npm beside it and falls back to
                                whatever npm is on the PATH; an older npm under
                                node 24 fails with the singularly unhelpful
                                "Class extends value undefined is not a
                                constructor or null".
                            -->
                            <nodeVersion>v24.19.0</nodeVersion>
                            <npmVersion>11.17.0</npmVersion>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Content package -->
            <plugin>
                <groupId>io.wcm.maven.plugins</groupId>
                <artifactId>wcmio-content-package-maven-plugin</artifactId>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>package</goal>
                        </goals>
                        <configuration>
                            <embeddedTarget>/apps/bayviewthemevue3/install</embeddedTarget>
                            <embeddeds>
                                <embedded>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>bayviewthemevue3-core</artifactId>
                                    <generateFilter>true</generateFilter>
                                </embedded>
                            </embeddeds>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>bayviewthemevue3-core</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>autoInstallPackage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.wcm.maven.plugins</groupId>
                        <artifactId>wcmio-content-package-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>install-package</id>
                                <goals>
                                    <goal>install</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
