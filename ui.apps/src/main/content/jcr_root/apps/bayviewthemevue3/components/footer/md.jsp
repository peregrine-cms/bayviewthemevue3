<%@ page session="false" contentType="text/plain;charset=UTF-8" pageEncoding="UTF-8"
    import="org.apache.sling.api.resource.ValueMap,org.apache.sling.api.resource.Resource" %>
<%@ taglib prefix="sling" uri="http://sling.apache.org/taglibs/sling/1.0" %>
<sling:defineObjects/>
<%
  // Generated by percli hatch (mdgen) from model.json — do not edit by hand.
  // Renders the authored properties of a footer as Markdown for LLMs.
  ValueMap vm = resource.getValueMap();
  {
    String v = vm.get("address", "")
        .replaceAll("(?i)<br\\s*/?>", "\n").replaceAll("(?i)</p>", "\n\n")
        .replaceAll("<[^>]+>", " ").replaceAll("&nbsp;", " ").replaceAll("&amp;", "&")
        .replaceAll("[ \\t]+", " ").replaceAll("\\n{3,}", "\n\n").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
  {
    String v = vm.get("image", "").trim();
    if (!v.isEmpty()) out.write("\n![" + vm.get("imagealt", "") + "](" + v + ")\n\n");
  }
  {
    Resource rows = resource.getChild("legallinks");
    if (rows != null && rows.hasChildren()) {
      out.write("\n| " + "Name" + " | " + "Link" + " |\n");
      out.write("|---|---|\n");
      for (Resource row : rows.getChildren()) {
        ValueMap rv = row.getValueMap();
        out.write("| " + rv.get("name", "") + " | " + rv.get("link", "") + " |\n");
      }
      out.write("\n");
    }
  }
  {
    String v = vm.get("brand", "").replaceAll("<[^>]+>", " ").replaceAll("[ \\t]+", " ").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
  {
    String v = vm.get("tel", "").replaceAll("<[^>]+>", " ").replaceAll("[ \\t]+", " ").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
  {
    String v = vm.get("email", "").replaceAll("<[^>]+>", " ").replaceAll("[ \\t]+", " ").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
  {
    String v = vm.get("social", "").replaceAll("<[^>]+>", " ").replaceAll("[ \\t]+", " ").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
  {
    String v = vm.get("copyright", "").replaceAll("<[^>]+>", " ").replaceAll("[ \\t]+", " ").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
  {
    String v = vm.get("videourl", "").replaceAll("<[^>]+>", " ").replaceAll("[ \\t]+", " ").trim();
    if (!v.isEmpty()) out.write("\n" + v + "\n\n");
  }
%>
